# Slack Threads: десктопное приложение для чтения тредов с переводом на русский

## Overview

Десктопное приложение (Go + Wails) для работы с отдельными Slack-тредами на русском языке.

Проблема: рабочие треды в англоязычных каналах длинные и технические. Читать их через
Slack медленно, а разовый перевод в чате не сохраняется и не обновляется.

Что делает приложение:
- тред добавляется по permalink и сохраняется локально навсегда (SQLite)
- при добавлении тред целиком переводится на русский через Claude CLI с сохранением
  контекста всего обсуждения (одна долгоживущая сессия на тред)
- кнопка «Обновить» на треде и «Обновить все» перечитывают треды и переводят только
  изменившееся; автоматического поллинга нет
- для каждого треда генерируется блок «Суть» (о чём тред, что ждут от пользователя)
- ответ пишется по-русски, переводится на английский, показывается обратный перевод
  для контроля, отправляется в тред через `chat.postMessage`
- тред можно удалить (со всеми сообщениями и переводами) или архивировать
  (перестать обновлять, перевод сохранить)

Ключевые решения, принятые при планировании:
- **Slack-доступ**: собственный Slack app с user-токеном (`xoxp-`), токен хранится в
  системном keychain. Создание приложения и получение апрува в workspace — ручной шаг
  пользователя (см. Post-Completion)
- **Переводчик**: бинарник `claude` как подпроцесс, долгоживущая сессия на тред через
  `stream-json` по stdin/stdout. Качество Opus, оплата по подписке, API-ключ не нужен
- **Обновление**: полное перечитывание треда с пагинацией и сверка по `ts` + хешу
  текста — ловит и новые сообщения, и правки старых
- **UI**: Wails (Go-бэкенд + webview-фронт), потому что контент — форматированный текст
  с код-блоками, ссылками и списками

## Context (from discovery)

- Проект создаётся с нуля в `~/repo/slack-threads` — существующей кодовой базы нет
- Окружение: Go 1.26.0 (darwin/arm64), `ralphex` установлен, `claude` CLI 2.1.267
- Wails не установлен — ставится через `go install github.com/wailsapp/wails/v2/cmd/wails@latest`
- Локального Slack-токена в системе нет, `slck` не установлен. MCP-коннектор claude.ai
  приложению недоступен — нужен собственный токен
- Правила перевода уже сформулированы в скилле `~/.claude/skills/slack-thread/`
  (`SKILL.md` разделы 3-4 и `glossary.md`) — их нужно перенести в системный промпт
  переводчика, а не изобретать заново

## Development Approach

- **Testing approach**: TDD — каждая задача начинается с падающего теста
- Complete each task fully before moving to the next
- Make small, focused changes
- **CRITICAL: every task MUST include new/updated tests** for code changes in that task
  - tests are not optional - they are a required part of the checklist
  - write unit tests for new functions/methods
  - write unit tests for modified functions/methods
  - add new test cases for new code paths
  - update existing test cases if behavior changes
  - tests cover both success and error scenarios
- **CRITICAL: all tests must pass before starting next task** - no exceptions
- **CRITICAL: update this plan file when scope changes during implementation**
- Run tests after each change
- Maintain backward compatibility

Дополнительно для этого проекта:
- внешние зависимости (Slack HTTP API, бинарник `claude`) закрываются интерфейсами и
  подменяются в тестах — тесты не ходят в сеть и не запускают `claude`
- SQLite через `modernc.org/sqlite` (чистый Go, без cgo) — кросс-компиляция и простая сборка

## Testing Strategy

- **Unit tests**: required for every task (see Development Approach above)
- Slack-клиент тестируется против `httptest.Server` с записанными формами ответов
- Переводчик тестируется против фейкового исполняемого файла (тестовый скрипт,
  печатающий заранее заданные stream-json события), интерфейс `Runner` подменяется
- Store тестируется на реальной SQLite-базе во временной директории
- **E2E tests**: UI-фреймворка для e2e в проекте нет; проверка UI — ручная, сценарии
  перечислены в Post-Completion

## Progress Tracking

- Mark completed items with `[x]` immediately when done
- Add newly discovered tasks with ➕ prefix
- Document issues/blockers with ⚠️ prefix
- Update plan if implementation deviates from original scope
- Keep plan in sync with actual work done

## What Goes Where

- **Implementation Steps** (`[ ]` checkboxes): код, тесты, документация в этом репозитории
- **Post-Completion** (no checkboxes): создание Slack app, апрув в workspace, ручная
  проверка UI, сборка `.app`

## Implementation Steps

### Task 1: Каркас проекта и проверка контрактов внешних инструментов

Спайк: до написания кода зафиксировать реальные интерфейсы CLI и Slack API, чтобы не
строить обвязку на предположениях.

- [x] выполнить `claude --help` и записать в `docs/claude-cli-contract.md` фактические
      флаги: `-p`, `--input-format stream-json`, `--output-format stream-json`,
      `--verbose`, `--append-system-prompt`, `--model`, `--resume`, флаги ограничения
      инструментов и режима разрешений
- [x] запустить `claude` вручную в режиме stream-json, отправить одно сообщение и
      записать в тот же документ реальные формы событий stdin/stdout (`user`,
      `assistant`, `result`, поле `session_id`)
- [x] `go mod init github.com/pavelvarganov/slack-threads`, установить Wails CLI,
      сгенерировать каркас `wails init` с vanilla-фронтендом
- [x] создать структуру каталогов: `cmd/slack-threads`, `internal/permalink`,
      `internal/store`, `internal/slackapi`, `internal/translate`, `internal/sync`,
      `internal/app`, `frontend`
- [x] добавить `Makefile` (цели `test`, `lint`, `build`, `dev`) и `.golangci.yml`
- [x] добавить smoke-тест, проверяющий что пакеты собираются (`go build ./...` в CI-цели)
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- для изоляции переводчика нужны **оба** флага `--tools "" --strict-mcp-config`:
  без второго MCP-серверы пользователя подключаются даже при пустом списке инструментов
- Wails собирает корень модуля, поэтому точка входа продублирована: `main.go` в корне
  (нужен `wails build`) и `cmd/slack-threads/main.go` — оба трёхстрочные обёртки над
  `internal/app.Run`; встраивание ассетов вынесено в пакет `frontend`
- добавлен `internal/config` (в списке каталогов его не было, но он есть в Technical Details)
- smoke-тест живёт в `internal/buildsmoke` и прогоняет `go build ./...` и `go vet ./...`

### Task 2: Разбор Slack permalink

- [x] написать тесты для `permalink.Parse` (табличные): ссылка на корневое сообщение,
      ссылка с `thread_ts` в query, ссылка на канал `app.slack.com/client/...`,
      приватная группа `G…`, личка `D…`, мусор и пустая строка
- [x] написать тест на конвертацию `p1788872615903009` → `1788872615.903009`
- [x] реализовать `internal/permalink`: возврат `channelID`, `threadTS`, `focusTS`,
      `workspace` и типизированные ошибки для нераспознанной ссылки
- [x] написать тесты на обратную сборку permalink по `channelID` + `ts` (нужно для
      ссылки на отправленное сообщение)
- [x] run tests - must pass before next task

### Task 3: Хранилище — схема, миграции, треды

- [x] написать тесты для `store.Open` на временной базе: создание схемы, идемпотентность
      повторного открытия, применение миграций по версии
- [x] реализовать `internal/store` на `modernc.org/sqlite` с версионированными миграциями
- [x] описать схему: `threads` (id, channel_id, thread_ts, workspace, title, added_at,
      last_fetched_at, archived, claude_session_id), `messages` (id, thread_id, ts,
      user_id, text, raw_json, edited_ts, text_hash), `translations` (message_id, text_ru,
      model, created_at), `summaries` (thread_id, text_ru, based_on_ts, updated_at),
      `users` (id, display_name, real_name, is_bot, updated_at), `drafts` (thread_id,
      text_ru, text_en, back_ru, updated_at)
- [x] написать тесты на CRUD тредов: добавление, список, получение, архивирование,
      удаление с каскадом (сообщения, переводы, суть, черновик уходят вместе)
- [x] написать тест на уникальность треда по паре (channel_id, thread_ts)
- [x] реализовать методы работы с тредами
- [x] run tests - must pass before next task

### Task 4: Хранилище — сообщения, переводы, пользователи

- [x] написать тесты на upsert сообщений: новое сообщение вставляется, неизменённое не
      трогается, изменённый текст обновляет запись и инвалидирует перевод
- [x] написать тесты на вычисление `text_hash` и на выборку «сообщения без актуального
      перевода»
- [x] реализовать upsert сообщений с инвалидацией переводов по хешу
- [x] написать тесты на сохранение и чтение переводов, сути и кэша пользователей
- [x] реализовать соответствующие методы
- [x] run tests - must pass before next task

### Task 5: Slack-клиент — чтение треда

- [x] написать тесты на `conversations.replies` против `httptest.Server`: одна страница,
      пагинация по `response_metadata.next_cursor`, пустой тред, `ok:false` с
      `error: channel_not_found`
- [x] написать тесты на обработку `429`: уважение `Retry-After`, ограниченное число
      повторов, возврат типизированной ошибки при исчерпании
- [x] реализовать `internal/slackapi` с интерфейсом `Client`, транспортом на
      `net/http`, user-токеном в заголовке `Authorization: Bearer`
- [x] реализовать `FetchThread(ctx, channelID, threadTS)` с полной пагинацией
- [x] написать тесты на маппинг сырого ответа в доменную структуру сообщения
      (ts, user, bot_id, text, edited.ts, reactions, subtype)
- [x] run tests - must pass before next task

### Task 6: Slack-клиент — пользователи и рендер mrkdwn

- [x] написать тесты на `users.info` с кэшированием в store: повторный запрос того же
      id не ходит в сеть, неизвестный пользователь не роняет загрузку треда
- [x] реализовать разрешение имён пользователей и ботов с кэшем
- [x] написать тесты для конвертера Slack mrkdwn: `<http://url|label>`, `<@U123>`,
      `<#C123|name>`, HTML-энтити `&amp; &lt; &gt;`, тройные бэктики, инлайн-код,
      вложенные и многострочные случаи
- [x] реализовать конвертер mrkdwn → структурированный вид для фронтенда
- [x] run tests - must pass before next task

### Task 7: Переводчик — управление сессией claude

- [x] написать тесты на `translate.Session` с подменённым `Runner`: запуск процесса,
      отправка сообщения, чтение ответа до события `result`, извлечение `session_id`
- [x] написать тесты на ошибки: процесс завершился с ненулевым кодом, невалидный JSON в
      stdout, таймаут ответа, отмена по context
- [x] реализовать `internal/translate` с интерфейсом `Runner` (реальная реализация —
      `os/exec` с `claude`) и менеджером сессий: одна сессия на тред, ленивый запуск,
      закрытие по неактивности, переиспользование `session_id` через `--resume`
- [x] написать тесты на то, что сессия переиспользуется для того же треда и не
      переиспользуется для другого
- [x] реализовать ограничение инструментов и permission-mode для процесса-переводчика
      (переводчику не нужны файловые и сетевые инструменты)
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- изоляция процесса: `--tools "" --strict-mcp-config --permission-mode dontAsk
  --permission-prompts none` (сборка аргументов в `buildArgs`, покрыта табличным тестом)
- закрытие по неактивности сделано ленивым: `Manager.CloseIdle()` плюс автоматическая
  чистка на каждом обращении к `Manager.Session` — фонового таймера нет, время
  подменяется в тестах через `Manager.now`
- любая ошибка хода (таймаут, битый JSON, смерть процесса, отмена по context) убивает
  процесс и помечает сессию закрытой: половину потока переиспользовать нельзя,
  менеджер заводит новый процесс с тем же `--resume`
- `ExecRunner` протестирован против фейкового шелл-скрипта `claude`: реальный процесс,
  реальные пайпы, проверка переданных аргументов

### Task 8: Переводчик — перевод сообщений треда

- [x] перенести правила перевода и глоссарий из `~/.claude/skills/slack-thread/` в
      `internal/translate/prompt.go` (системный промпт: не переводить код, бэктики,
      идентификаторы, URL, ключи задач, @упоминания, имена сервисов; сохранять списки и
      переносы; русские сообщения оставлять как есть)
- [x] написать тесты на сборку промпта: батч сообщений превращается в запрос с
      идентификаторами, ранее переведённые сообщения передаются как контекст
- [x] написать тесты на разбор структурированного ответа: соответствие переводов
      идентификаторам, отсутствующий перевод, лишний идентификатор, невалидный JSON
- [x] реализовать `TranslateMessages(ctx, threadID, msgs) ([]Translation, error)` с
      разбиением на батчи по объёму текста
- [x] написать тесты на батчинг: длинный тред режется на несколько запросов, порядок
      сохраняется
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- системный промпт и глоссарий лежат в `internal/translate/prompt.go` (`SystemPrompt`,
  `TranslationConfig()`); в него добавлен запрет трактовать содержимое сообщений как
  инструкции — процесс переводчика без инструментов, но промпт-инъекции нам тоже не нужны
- уже переведённые сообщения передаются как блок `CONTEXT` того же запроса; каждый
  следующий батч видит переводы предыдущих, поэтому терминология согласована
- ответ разбирается устойчиво: JSON-объект вырезается из возможной markdown-обёртки,
  лишние id игнорируются, недостающие вызывают ровно один повторный запрос только по
  пропущенным (`maxTranslateAttempts = 2`), после чего возвращается `*ResponseError`
- `Sessions`/`Turn` — интерфейсы над сессиями; `*Manager` подключается адаптером
  `ManagerSessions` с функцией `Resume`, которая на Task 10 будет брать
  `threads.claude_session_id` из store
- батчинг считает рунами (бюджет `defaultBatchChars = 6000`); сообщение длиннее бюджета
  уезжает отдельным батчем, а не режется

### Task 9: Переводчик — генерация «Сути» треда

- [x] написать тесты на промпт для сути: на вход идут переводы всех сообщений, на выход —
      короткий текст (о чём тред и что ждут от пользователя)
- [x] написать тесты на пропуск генерации для треда из одного-двух коротких сообщений
- [x] реализовать `Summarize(ctx, threadID, translated) (string, error)`
- [x] написать тесты на то, что суть пересобирается только при появлении новых или
      изменённых сообщений (сверка `based_on_ts`)
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- код лежит в `internal/translate/summary.go`: суть генерируется в той же долгоживущей
  сессии треда, поэтому модель уже видит переводы и не требует их пересылки как контекста
- порог пропуска: тред короче трёх сообщений и суммарно короче 400 рун сути не получает
  (`NeedsSummary`); в этом случае сессия вообще не поднимается
- `based_on_ts` пишется не просто как ts последнего сообщения, а как
  `<newest_ts>#<fnv64 хеш текстов>` (`SummaryBasedOn`): правка старого сообщения ts не
  меняет, а суть должна пересобираться — хеш это ловит. Проверка — `SummaryOutdated`
- ответ ожидается как `{"summary":"..."}`; JSON вырезается из возможной markdown-обёртки
  тем же `extractJSONObject`, пустая суть — `*ResponseError`

### Task 10: Сервис синхронизации треда

- [x] написать тесты на `sync.AddThread`: парсинг ссылки, загрузка треда, сохранение,
      перевод всех сообщений, генерация сути, повторное добавление того же треда не
      создаёт дубль
- [x] написать тесты на `sync.RefreshThread`: новые сообщения переводятся, неизменённые
      не перепереводятся, изменённое сообщение перепереводится, удалённое в Slack
      помечается в базе
- [x] написать тесты на `sync.RefreshAll`: обход неархивированных тредов, ошибка в одном
      треде не прерывает остальные, результат содержит per-thread статусы
- [x] реализовать `internal/sync` поверх store, slackapi и translate
- [x] написать тесты на прогресс-события (загрузка → перевод N из M → суть) для UI
- [x] реализовать канал прогресс-событий
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- пункта «помечается в базе» нельзя было выполнить на схеме из Task 3: добавлена
  миграция 2 (`messages.deleted`), метод `store.MarkMessagesDeleted` и поле
  `store.Message.Deleted`. Сообщение, исчезнувшее из Slack, не удаляется — только
  помечается, перевод сохраняется; вернувшееся сообщение снимает флаг
  (`UpsertMessages` считает это «updated`), а `UntranslatedMessages` удалённые не отдаёт
- прогресс отдаётся буферизованным каналом `Service.Progress()`; отправка
  неблокирующая — если UI не читает, событие теряется, но синхронизация не встаёт
- гранулярность «перевод N из M» задаётся `WithChunkSize` (по умолчанию 10 сообщений
  на запрос): `translate.TranslateMessages` сам не сообщает прогресс, поэтому дельта
  режется на чанки, и каждый запрос получает уже переведённое как контекст терминов
- заголовок треда генерируется из корневого сообщения (первая строка рендера mrkdwn,
  80 рун) один раз — при первом успешном чтении
- сообщения без текста (`channel_join` и прочие сабтайпы) сохраняются, но не переводятся
- сохранение `claude_session_id` в тред здесь не делается: `ManagerSessions.Resume`
  подключается на уровне биндингов в Task 13

### Task 11: Ответы — перевод RU→EN и отправка

- [x] написать тесты на `translate.DraftReply`: русский текст → английский + обратный
      перевод на русский, сохранение кода, идентификаторов, @упоминаний и эмодзи
- [x] реализовать `DraftReply(ctx, threadID, ru) (en, backRU string, err error)`
- [x] написать тесты на сохранение и чтение черновика в store
- [x] написать тесты на `chat.postMessage` против `httptest.Server`: успешная отправка в
      тред с `thread_ts`, ошибка внешнего Slack Connect канала, `not_in_channel`,
      `ratelimited`
- [x] реализовать отправку и возврат permalink отправленного сообщения
- [x] написать тесты на то, что после успешной отправки черновик очищается, а тред
      помечается требующим обновления
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- `translate.DraftReply` (`internal/translate/reply.go`) идёт в той же долгоживущей
  сессии треда, поэтому ответ подхватывает термины и тон обсуждения. Ответ ожидается как
  `{"text_en":"...","back_ru":"..."}`; обратный перевод модель делает по английскому
  тексту, а не по исходному русскому — иначе он не показывал бы, что реально уходит
- черновик хранится в готовой с Task 3 таблице `drafts`: `store.SaveDraft`/`GetDraft`/
  `DeleteDraft` (`internal/store/drafts.go`), один черновик на тред
- ➕ «тред помечается требующим обновления» потребовало нового поля: миграция 3
  (`threads.needs_refresh`), `store.SetThreadNeedsRefresh`, `store.Thread.NeedsRefresh`.
  Флаг ставится после успешной отправки и снимается первым же успешным `syncThread`
- в `slackapi` добавлен POST-транспорт (`c.post`): ретраи на `429` и разбор конверта
  общие с GET, тело запроса пересобирается на каждой попытке
- `chat.postMessage` не возвращает permalink, поэтому он берётся отдельным вызовом
  `chat.getPermalink`; ошибка этого вызова не валит отправку — сообщение уже в Slack
- коды отказа по политике (`restricted_action*`, `slack_connect*` — внешние каналы
  Slack Connect) заворачиваются в `slackapi.RestrictedError` с сентинелом
  `ErrPostRestricted`; сырой код Slack остаётся доступен через `errors.As(*APIError)`.
  Точный список кодов Slack не документирует стабильно — сверяется эмпирически
- `sync.SendReply(ctx, threadID, en)` принимает английский текст параметром, а не читает
  его из черновика: пользователь может поправить английский перед отправкой. При ошибке
  Slack черновик не удаляется — будет что повторить

### Task 12: Конфигурация и хранение токена

- [x] написать тесты на загрузку конфигурации: путь к базе, путь к бинарнику `claude`,
      модель, таймауты, значения по умолчанию
- [x] написать тесты на работу с токеном через подменяемый интерфейс `TokenStore`:
      сохранение, чтение, отсутствие токена
- [x] реализовать `internal/config` и хранение токена в системном keychain
      (`github.com/zalando/go-keyring`), с фолбэком на переменную окружения
- [x] написать тесты на валидацию токена (формат `xoxp-`, проверка через `auth.test`)
- [x] реализовать проверку токена при старте с понятной ошибкой в UI
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- `auth.test` добавлен как метод `slackapi.HTTPClient.AuthTest`, но **не** в интерфейс
  `slackapi.Client`: проверка токена — задача старта приложения, а не потока синхронизации,
  и расширение интерфейса ломало бы существующие фейки
- хранилище токена собрано из трёх частей: `KeyringStore` (системная связка ключей),
  `EnvStore` (только чтение, `SLACK_THREADS_TOKEN`) и `ChainStore`, который читает из
  первого непустого и пишет в первый доступный на запись. Порядок в `NewTokenStore` —
  окружение, затем keychain: переменная перекрывает сохранённый токен на один запуск,
  не затирая его
- проверка при старте живёт в `internal/app`: `startup` вызывает `checkToken`, результат
  кэшируется и отдаётся во фронтенд как `TokenStatus{ok, user, team, message, fixable}`;
  `fixable` отличает «введите другой токен» от «повторите позже» (rate limit, заблокированная
  связка ключей). Сами тексты ошибок — русские, формируются в `config.TokenProblem`
- `internal/app.New` стал вариативным (`New(opts ...Option)`) — это нужно уже сейчас для
  подмены keychain и Slack в тестах; в Task 13 через те же опции пройдут остальные зависимости

### Task 13: Wails-биндинги

- [x] написать тесты на методы `internal/app`: `AddThread(url)`, `ListThreads()`,
      `GetThread(id)`, `RefreshThread(id)`, `RefreshAll()`, `DeleteThread(id)`,
      `ArchiveThread(id)`, `DraftReply(id, ru)`, `SendReply(id, en)`, `SaveToken(t)`
- [x] написать тесты на то, что ошибки доменного слоя превращаются в понятные
      пользователю сообщения, а не в сырые тексты ошибок
- [x] реализовать биндинги и проброс прогресс-событий во фронтенд через Wails events
- [x] написать тесты на защиту от параллельного обновления одного треда
- [x] реализовать блокировку по треду
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- биндинги разложены по файлам: `internal/app/threads.go` (методы), `views.go` (DTO
  для фронтенда), `errors.go` (перевод доменных ошибок), `locks.go` (блокировки),
  `wire.go` (продакшн-сборка зависимостей)
- ошибки уезжают во фронтенд через `*app.UserError`: Wails отдаёт JS только текст
  ошибки, поэтому этот текст сразу и есть сообщение для пользователя. Причина
  сохраняется в `Err` (работает `errors.Is`), а флаг `Fixable` говорит UI, можно ли
  что-то сделать прямо сейчас (ввести токен, вступить в канал, повторить)
- ➕ `ArchiveThread(id, archived bool)` вместо `ArchiveThread(id)`: разархивирование
  нужно тем же методом, иначе архив станет односторонним
- ➕ `ListThreads()` отдаёт и архивные треды с флагом `archived` — фильтрация это
  задача списка слева, а не бэкенда
- прогресс: `sync.Progress` пробрасывается в событие `sync:progress`
  (`app.EventProgress`) через подменяемый `Emitter`, по умолчанию —
  `wailsruntime.EventsEmit`. Насос запускается один раз при первом успешном
  подключении токена и живёт до закрытия окна
- блокировки — набор именованных замков (`keyLock`): ключ треда для
  `RefreshThread`/`DeleteThread`, ключ ссылки для `AddThread` (ID ещё неизвестен),
  эксклюзивный ключ `*` для `RefreshAll`. Второй вызов не встаёт в очередь, а сразу
  возвращает `ErrBusy` — иначе двойной клик удваивал бы трафик Slack и переводы
- сборка зависимостей привязана к токену: Slack-клиент, менеджер сессий claude и
  `sync.Service` создаются в `App.wire` после успешного `auth.test`, поэтому смена
  токена в UI пересобирает их без перезапуска. Пока токена нет, биндинги отвечают
  «приложение не готово»
- `Run` теперь открывает базу (`store.Open`) и передаёт конфиг в `App`;
  `store.NewUserCache` подключается как кеш профилей Slack-клиента

### Task 14: Интерфейс

- [x] сверстать двухколоночный макет: слева список тредов с кнопками «Добавить по
      ссылке» и «Обновить все», справа лента треда
- [x] реализовать ленту: «Суть» вверху, сообщения с именем и временем, перевод на
      русском, тумблер «показать оригинал», код-блоки моноширинным с горизонтальным
      скроллом, реакции как есть
- [x] реализовать панель ответа: русский текст → «Перевести» → английский и обратный
      перевод → «Отправить», с блокировкой отправки до перевода
- [x] реализовать индикаторы: прогресс загрузки и перевода, состояние «нет токена»,
      ошибки Slack и переводчика
- [x] реализовать удаление и архивирование треда с подтверждением
- [x] проверить, что окно и лента корректно ведут себя в тёмной и светлой теме macOS
      (реализовано через `prefers-color-scheme` и переменные темы; визуальная сверка —
      ручной сценарий в Post-Completion)
- [x] run tests - must pass before next task

➕ Уточнения по факту реализации:
- фронтенд разложен на модули: `src/format.js` (рендер блоков, времени, прогресса),
  `src/state.js` (состояние и переходы), `src/view.js` (разметка строками HTML),
  `src/api.js` (мост к Wails-биндингам и событиям), `src/main.js` (контроллер и DOM).
  `format`/`state`/`view` — чистые функции, поэтому покрыты unit-тестами
- ➕ тесты фронтенда: `node --test` (без внешних зависимостей), 27 тестов в
  `frontend/test/`; `make test` теперь прогоняет `test-go` и `test-front`
- ➕ рендер mrkdwn перенесён на бэкенд: `MessageView` получил `blocks`/`blocksRu`, а
  `ThreadView` — `summaryBlocks`, собранные уже существующим `slackapi.RenderMrkdwn`
  с картой имён пользователей. Иначе пришлось бы держать второй парсер Slack-разметки
  в JS
- отправка блокируется не только пустым английским текстом, но и правкой русского
  после перевода (`translatedFrom` в черновике): иначе в тред уехал бы текст, который
  пользователь не проверял обратным переводом
- перерисовка идёт целиком через `innerHTML`, поэтому после неё возвращаются фокус и
  каретка активного текстового поля — событие прогресса не должно сбивать набор ответа
- ввод в textarea не вызывает перерисовку (только запоминается в state); кнопки
  пересчитываются по `blur` и по действиям

### Task 15: Verify acceptance criteria

- [ ] verify all requirements from Overview are implemented
- [ ] verify edge cases are handled: тред без прав доступа, удалённый тред, тред только
      с ботами, очень длинный тред с пагинацией, недоступный бинарник `claude`,
      протухший токен, обрыв сети посреди обновления
- [ ] run full test suite (unit tests)
- [ ] run linter - all issues must be fixed
- [ ] verify test coverage meets project standard (80%+)

### Task 16: [Final] Update documentation

- [ ] написать `README.md`: назначение, установка, создание Slack app и список scopes,
      первый запуск, сборка `.app`
- [ ] зафиксировать в `docs/` контракт `claude` CLI (из Task 1) и схему базы
- [ ] описать известные ограничения: отсутствие автополлинга, лимиты Slack на
      `conversations.replies`, невозможность отправки в Slack Connect каналы

## Technical Details

**Структура проекта**

```
~/repo/slack-threads/
  cmd/slack-threads/main.go     точка входа, сборка зависимостей, запуск Wails
  internal/permalink/           разбор и сборка ссылок Slack
  internal/store/               SQLite: схема, миграции, репозитории
  internal/slackapi/            HTTP-клиент Slack: replies, users.info, postMessage
  internal/translate/           сессии claude, промпты, перевод, суть, черновики ответов
  internal/sync/                оркестрация: добавить, обновить один, обновить все
  internal/config/              конфиг и токен (keychain)
  internal/app/                 Wails-биндинги
  frontend/                     HTML/CSS/JS
  docs/plans/                   планы
```

**Поток «добавить тред»**

permalink → `permalink.Parse` → `slackapi.FetchThread` (с пагинацией) →
`store.UpsertMessages` → `slackapi.ResolveUsers` → `translate.TranslateMessages`
(сессия треда, батчи) → `store.SaveTranslations` → `translate.Summarize` →
`store.SaveSummary`. Прогресс транслируется в UI на каждом шаге.

**Поток «обновить»**

Тред перечитывается целиком. Для каждого сообщения считается `text_hash`; новые и
изменившиеся попадают в очередь перевода, остальные не трогаются. Перевод дельты идёт в
ту же сессию треда, поэтому термины согласованы с уже переведённой частью. Суть
пересобирается только если дельта непустая.

**Протокол общения с claude**

Долгоживущий процесс на тред:
`claude -p --input-format stream-json --output-format stream-json --verbose
--append-system-prompt <правила перевода> --model opus`.
Запросы пишутся в stdin JSON-строками, ответы читаются из stdout до события `result`.
`session_id` из первого ответа сохраняется в `threads.claude_session_id` и используется
для `--resume` после перезапуска приложения. Точные формы событий фиксируются в Task 1 и
не додумываются по памяти.

**Формат запроса на перевод**

На вход модели идёт JSON-массив `{id, author, text}`, на выход ожидается
`{id, text_ru}` для каждого элемента. Соответствие проверяется по id; недостающие
переводы вызывают повторный запрос только для пропущенных сообщений.

**Ограничения Slack**

`conversations.replies` для приложений не из Marketplace ограничен примерно одним
запросом в минуту и небольшим числом сообщений в ответе. Пагинация длинного треда при
первом импорте поэтому медленная: клиент должен уважать `Retry-After`, показывать
прогресс и не считать `429` фатальной ошибкой. Точное поведение для user-токена
проверяется эмпирически на живом токене (см. Post-Completion).

## Post-Completion

*Items requiring manual intervention or external systems - no checkboxes, informational only*

**Внешние шаги перед первым запуском**

- Создать Slack app на api.slack.com/apps в workspace overgearcom
- Запросить User Token Scopes: `channels:history`, `groups:history`, `im:history`,
  `mpim:history`, `users:read`, `chat:write`
- Установить приложение в workspace; если самостоятельная установка запрещена —
  получить апрув администратора workspace
- Сохранить полученный `xoxp-` токен в приложении (он ляжет в системный keychain)

**Ручная проверка**

- Добавить реальный длинный тред с код-блоками и проверить качество перевода и
  сохранность кода, идентификаторов и ссылок
- Дописать сообщение в тред из Slack и проверить, что «Обновить» подтягивает и
  переводит только его
- Отредактировать старое сообщение в Slack и проверить, что перевод обновился
- Отправить ответ из приложения и убедиться, что он попал в тред, а не в канал
- Проверить поведение на внешнем (Slack Connect) канале: отправка должна давать понятную
  ошибку с возможностью скопировать текст
- Проверить фактические лимиты `conversations.replies` для user-токена и при
  необходимости скорректировать стратегию пагинации

**Сборка и распространение**

- `wails build` для получения `.app`
- Подпись и нотаризация не планируются: приложение личное, запускается локально
